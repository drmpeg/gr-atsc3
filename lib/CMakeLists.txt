# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-atsc3
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND atsc3_sources
    bbscrambler_bb_impl.cc
    bch_bb_impl.cc
    ldpc_bb_impl.cc
    interleaver_bb_impl.cc
    modulator_bc_impl.cc
    framemapper_cc_impl.cc
    pilotgenerator_cc_impl.cc
    freqinterleaver_cc_impl.cc
)

set(atsc3_sources "${atsc3_sources}" PARENT_SCOPE)
if(NOT atsc3_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT atsc3_sources)

add_library(gnuradio-atsc3 SHARED ${atsc3_sources})
target_link_libraries(gnuradio-atsc3 gnuradio::gnuradio-runtime)
target_include_directories(gnuradio-atsc3
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-atsc3 PROPERTIES DEFINE_SYMBOL "gnuradio_atsc3_EXPORTS")

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall")

if(APPLE)
    set_target_properties(gnuradio-atsc3 PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-atsc3)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_atsc3_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-atsc3)

if(NOT test_atsc3_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_atsc3_sources)

foreach(qa_file ${test_atsc3_sources})
    GR_ADD_CPP_TEST("atsc3_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
